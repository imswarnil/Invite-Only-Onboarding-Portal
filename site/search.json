---
layout: null
permalink: /search.json
---
{% capture newline %}
{% endcapture %}
[
  {% assign lessons = site.learning | sort: "order" %}
  {% for lesson in lessons %}
  {
    "title": {{ lesson.title | jsonify }},
    "section": {{ lesson.section | jsonify }},
    "url": {{ lesson.url | relative_url | jsonify }},
    "excerpt": {{ lesson.content | split: "</h1>" | last | strip_html | replace: newline, " " | strip | truncate: 180 | jsonify }}
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]

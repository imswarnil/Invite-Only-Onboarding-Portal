<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
  Scoped manifest for THIS app only — named components only, no prefix.
  This org (go-to-market-ai-dev-ed) hosts other POCs — never widen this to
  wildcard members (<members>*</members>). Add named <members> here as each
  phase creates real components, then retrieve with:
    sf project retrieve start --manifest manifest/package.xml --target-org iop-dev
-->
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <types>
        <members>Invite_Only_Onboarding_Portal</members>
        <name>CustomApplication</name>
    </types>
    <types>
        <members>Homepage</members>
        <name>CustomTab</name>
    </types>
    <version>67.0</version>
</Package>
